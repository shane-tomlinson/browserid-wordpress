jQuery(document).ready(function ($) {
	'use strict';
	// $() will work as an alias for jQuery() inside of this function

	// add color picker to the background setting
	$('#browserid_background_color').wpColorPicker();
}); // doc rdy